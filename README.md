# Data-Centric PPE Detection in Extreme UAE Environments
### YOLOv8m vs RT-DETR-L, Two-Phase Study

**Dewansh Agrawal, Ahmed Alnuaimi, Abdullah Siddiqui**  
College of Engineering (CEN) - American University of Sharjah

---

## Overview

This project investigates automated PPE (Personal Protective Equipment) detection on UAE construction sites using a **two-phase, data-centric methodology**. Standard models fail under harsh Gulf conditions, Khamseen sandstorms, UV bleaching, heat shimmer, and intense directional shadows. Rather than changing model architectures, we systematically engineered the data to fix what the models could not learn.

**Phase 1** trains YOLOv8m and RT-DETR-L on a Roboflow construction site dataset with custom UAE weather augmentations, then stress-tests both models on empty desert backgrounds. Both hallucinate PPE on bare scaffolding and sand, a direct consequence of data starvation and zero negative training examples.

**Phase 2** fuses the Mendeley SHWD dataset (~5,000 helmet images), injects 129 explicit background images with empty labels, and doubles the training set via augmentation to produce a 12,274-image Mega-Dataset. The result: **1.000 recall on NO-Hardhat detection** on both models — every bare-head violation correctly flagged.

---

## Results

| Metric | YOLOv8m P1 | YOLOv8m P2 | RT-DETR-L P1 | RT-DETR-L P2 |
|---|---|---|---|---|
| mAP50 (overall) | 0.658 | 0.533 | 0.567 | **0.600** |
| Precision | 0.868 | 0.704 | 0.690 | **0.786** |
| Recall | 0.602 | 0.542 | 0.538 | **0.585** |
| NO-Hardhat Recall | 0.645 | **1.000** | 0.691 | **1.000** |
| Hardhat mAP50 (val) | — | 0.912 | — | 0.894 |
| Training time | 31.6 min | 173.3 min | 108.4 min | 94.1 min |
| Inference FPS | **43.0** | — | 22.1 | — |

*Overall metrics evaluated on the same held-out 82-image out-of-domain test set. Val figures use the 864-image SHWD-heavy validation set.*

---

## Repository Structure

```
/
├── PPE_Detection_UAE.ipynb          ← Main notebook — full pipeline
│
├── ppe-detection/                   ← Phase 1
│   ├── configs/dataset.yaml
│   ├── data/download_dataset.py     ← Roboflow downloader + yaml patcher
│   ├── requirements.txt
│   ├── src/
│   │   ├── augment.py               ← UAE augmentation pipeline
│   │   ├── train.py                 ← Unified training CLI (yolo | rtdetr)
│   │   ├── evaluate.py              ← Test-set evaluation → CSV
│   │   ├── compare.py               ← Comparison plots + FPS benchmark
│   │   ├── preprocess.py            ← Image validation + class distribution
│   │   └── visualize.py             ← GT vs YOLO vs RT-DETR prediction grid
│   ├── results/
│   │   ├── metrics/                 ← yolo_metrics.csv, rtdetr_metrics.csv
│   │   └── plots/                   ← class_distribution.png, comparison.png,
│   │                                   prediction_grid.png
│   └── runs/
│       ├── yolo/                    ← Training plots, curves, confusion matrix
│       ├── rtdetr/
│       ├── yolo_eval/               ← Test-set eval: BoxF1, PR, confusion matrix
│       └── rtdetr_eval/
│
└── ppe-detection-v2/                ← Phase 2
    ├── configs/phase2_dataset.yaml
    └── data/
        └── background_images/       ← 129 negative examples (desert + scaffolding)
```

> **Not tracked by Git** (see `.gitignore`):
> - `ppe-detection/data/construction_site_safety/` — download via notebook
> - `ppe-detection-v2/data/shwd/` — download from Mendeley (link in notebook Section 2)
> - `ppe-detection-v2/data/combined/` — generated by notebook Section 2.3
> - All `*.pt` weight files — saved to Google Drive during training

---

## How to Run — Google Colab (Recommended)

The entire pipeline runs from **`PPE_Detection_UAE.ipynb`**.

```
1. Open Colab → Runtime → Change runtime type → L4 GPU → Save
2. Upload PPE_Detection_UAE.ipynb (or open from Drive)
3. Run cells top to bottom
```

| Notebook section | What it does |
|---|---|
| Setup | Mount Drive, clone repo, install deps |
| Dataset | Download Roboflow dataset |
| Preprocessing | Validate images, class distribution chart |
| Augmentation | UAE pipeline demo → generate 1,500 images → quality filter → copy to train |
| Phase 1 Training | YOLOv8m (~25 min on L4) + RT-DETR-L (~90 min) |
| Phase 1 Evaluation | Per-class metrics, confusion matrices, PR curves, prediction grid |
| Phase 2 | SHWD conversion, background injection, Mega-Dataset build |
| Phase 2 Training | Both models on 12,274-image corpus |
| Phase 2 Evaluation | Out-of-domain test + Phase 1 vs 2 delta table |

## Augmentation Pipeline

Five custom transforms simulate Gulf construction site conditions (Albumentations 2.x):

| Transform | Simulates | Implementation |
|---|---|---|
| Khamseen dust overlay | Sandy haze, warm ochre colour cast | Custom `A.Lambda` |
| UV bleaching | Noon sun desaturation of hardhat/vest colours | Custom `A.Lambda` + HSV |
| Heat shimmer | Mirage distortion from 50°C asphalt | `A.GridDistortion` |
| Directional shadow | Harsh contrast from Gulf noon lighting | `A.RandomShadow` |
| Sun flare / overexposure | Direct solar glare on cameras | `A.RandomSunFlare` |

Quality filtering rejects augmented images with mean pixel < 25 (too dark), > 240 (blown out), or std < 8 (flat). 1,276 of 1,500 generated images passed and were added to training.

---

## Phase 2 Dataset Engineering

| Source | Train | Valid | Test |
|---|---|---|---|
| Roboflow Phase 1 | 1,797 | 114 | 82 |
| Mendeley SHWD | 4,250 | 750 | — |
| Background (negative) images | 129 | — | — |
| **Combined pre-augmentation** | **6,176** | **864** | **82** |
| **After offline augmentation** | **12,274** | **864** | **82** |

**SHWD Fusion** — VOC XML → YOLO conversion, class mapping: `helmet` → Hardhat (0), `head` → NO-Hardhat (3).

**Negative Example Mining** — 129 images of empty desert and bare scaffolding with empty label files, teaching the model that not every scene contains objects.

---

## Key Findings

**What worked:** Both Phase 2 models achieved 1.000 recall on NO-Hardhat detection on the original test set. RT-DETR-L responded more consistently to the intervention, gaining +3.3 mAP50 and +9.6 precision on the out-of-domain test, suggesting transformers generalise more robustly to dataset distribution shift.

**Trade-off discovered:** Hardhat precision collapsed after SHWD fusion (YOLO: 0.91 → 0.13). The model learned SHWD-style helmets well but over-fired on the original construction domain. Targeted hard negatives (helmet-like non-PPE objects) would be the correct next intervention.

**Deployment recommendation:** YOLOv8m Phase 1 for immediate use — 43 FPS, 32-minute training, highest accuracy on the original domain. RT-DETR-L for continued research iteration.

---

## Datasets

- [Construction Site Safety](https://universe.roboflow.com/roboflow-universe-projects/construction-site-safety) — Roboflow Universe
- [Safety Helmet Wearing Dataset (SHWD)](https://github.com/njvisionpower/Safety-Helmet-Wearing-Dataset) — Mendeley / GitHub
